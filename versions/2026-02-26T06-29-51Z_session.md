# Version Snapshot — 2026-02-26

## Files Changed
- `index.html`
- `globals.css`
- `data/assets/favicon.png` (new)
- `CNAME` (new)
- `Hero.jsx`
- `Projects.jsx`

---

## Changes Made This Session

### 1. GitHub Pages Deployment
- Created `CNAME` file with `manish-rv.com` for custom domain
- Merged `madeSiteReactive` branch into `main`
- Pushed to GitHub (repo renamed to `manish-rv.github.io`)
- Updated git remote URL

### 2. Projects Loading State
- Added animated "Loading..." text when `projects.items` is empty
- Three dots animate with staggered `dotPulse` keyframe (0.2s delay each)
- Added `@keyframes dotPulse` to `globals.css`
- Font size set to 39px (3x original)

### 3. "View All on GitHub" Link
- Increased font size from 10px to 15px in `Projects.jsx`
- Updated both `Projects.jsx` and `index.html`

### 4. Hero Section — Theme-Aware Text
- Changed h1 from hardcoded `text-white` to `text-skin-text`
- Changed lastName span from `text-[#f0ede6]/90` to `text-skin-text/90`
- Changed cursor underscore from `text-[#333]` to `text-skin-border`

### 5. Theme Toggle Icons
- Replaced `○` / `●` text with SVG icons
- Light mode active: black half-moon SVG (switch to dark)
- Dark mode active: yellow sun SVG `#f5c542` (switch to light)

### 6. Hero Section — Removed Full-Screen Height
- Removed `min-h-screen` from hero section
- Increased top padding to `pt-44` (Hero.jsx) / `pt-32 md:pt-36` (index.html)

### 7. Light Mode — Dark Blue Color Scheme
- Replaced all black text/button values with dark navy blues
  - `--text-primary`: `#0a0a0a` → `#0c1a3a`
  - `--text-secondary`: `#555` → `#2a3f6e`
  - `--text-tertiary`: `#777` → `#4a5d8a`
  - `--btn-bg`: `#0a0a0a` → `#0c1a3a`
- Updated border colors to dark blue (`#2a3f6e`, `#1e3060`, `#243868`)
- Updated cursor colors to dark navy
- Updated all RGB channel variables to match

### 8. Navigation Bar Improvements
- Increased nav link font size from 10px to 13px
- Increased CTA button font size from 10px to 13px
- Nav links now use `text-skin-text` (highlighted by default)
- Added animated underline on hover (`.nav-link::after` slides left to right)
- Nav bar background: dark mode `rgba(18,18,22,0.92)`, light mode `rgba(238,236,230,0.92)`

### 9. Custom Cursor — Removed Ring
- Removed the trailing circle/ring element from Cursor component
- Removed ring CSS (`.cursor-ring`) from `globals.css`
- Removed ring tracking JS (rx/ry interpolation)
- Kept crosshair and dot only
- Added `cursor: none` to `a, button, [role="button"]` to hide finger pointer on hover

### 10. CSS Bug Fix — Body Styles
- Fixed accidental split of `body` CSS block
- `background`, `color`, and `transition` properties were mistakenly moved to `a, button` rule
- Restored them to `body` — this fixed theme toggling for the entire site

### 11. Thicker Borders
- Section dividers: `border-b` → `border-b-2`
- Nav bottom border: `border-b` → `border-b-2`
- Marquee borders: `border-t border-b` → `border-t-2 border-b-2`
- CSS hairlines: 1px → 2px
- Added `border-color: var(--border-primary)` to all `a`, `button` elements

### 12. Nav Scroll Border — Theme-Aware
- Replaced hardcoded `rgba()` values in scroll handler with `var(--border-primary)`
- Border now shows/hides based on scroll position and matches theme

### 13. Circular Favicon
- Generated circular favicon from `logo_white.png` using Pillow
- White MRV logo on black circle with transparent edges
- Saved as `data/assets/favicon.png`
- Updated `<link rel="icon">` and theme flash script to use it

### 14. Theme-Aware Nav Logo
- Nav logo switches between `logo_black.png` (light mode) and `logo_white.png` (dark mode)
- Uses `MutationObserver` on `data-theme` attribute for real-time switching
- Clicking logo smoothly scrolls to top of page
